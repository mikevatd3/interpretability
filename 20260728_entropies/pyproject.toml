[project]
name = "entropies"
version = "0.1.0"
description = "Next-token entropy visualization for GPT-2 over successive sentence prefixes"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
    "plotly>=5.20",
    "pandas>=2.0",
    "numpy>=1.24",
    "python-dotenv>=1.0",
]
